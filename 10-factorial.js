const args = process.argv[2];
const num = Number(args);
const factorial=num =>{
    i=1
    while (i<=num){
        let total =0
        total *= i
    }
    console.log(`The factorial is ${total}`)
}